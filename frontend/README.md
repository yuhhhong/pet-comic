# Pet Comic Frontend

Install dependencies with `npm install`, then run:

```powershell
npm run dev
```

The Next.js App Router pages are intentionally skeletons until the backend APIs are implemented.
