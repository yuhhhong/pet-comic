# Pet Comic Backend

Install dependencies with `uv sync` from this directory, then run:

```powershell
uv run uvicorn app.main:app --reload
```

The current server only exposes `GET /health`; business APIs will be added in later steps.

